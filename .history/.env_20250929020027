# ST_Faktura environment configuration
# Fill in real values before running scripts that send email.

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Authentication method for Google Sheets (service_account or oauth)
AUTH_METHOD=service_account

# SMTP / Email settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=sune@stdigital.dk
# Choose 'password' (default) or 'oauth' for Gmail OAuth XOAUTH2
EMAIL_AUTH_METHOD=oauth
SENDER_EMAIL=sune@stdigital.dk
# SENDER_PASSWORD is ignored in oauth mode (you can remove or comment it)

# If using OAuth (EMAIL_AUTH_METHOD=oauth):
# 1. Place Gmail OAuth client JSON as gmail_credentials.json (or set GMAIL_CLIENT_SECRET_FILE)
# 2. First run will open browser to grant access; token stored in gmail_token.json (override via GMAIL_TOKEN_FILE)
# GMAIL_CLIENT_SECRET_FILE=gmail_credentials.json
# GMAIL_TOKEN_FILE=gmail_token.json

# Optional: override bookkeeping email (default hardcoded if not set)
BOOKKEEPING_EMAIL=indtaegt@ebogholderen.dk

# Optional: payment terms (days). If omitted defaults to 8
# PAYMENT_TERMS_DAYS=8

# (Optional) Future flags
# ENABLE_EMAIL=true
# SMTP_USE_SSL=false
