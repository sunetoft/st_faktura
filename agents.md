# Agent Notes

Use the deploy script for Cloud Run deployments.

- Run: ./deploy.sh
- The script uses .gcloudignore to exclude PDFs and other binary blobs.
- Optional env overrides: PROJECT_ID, REGION, SERVICE_NAME.
